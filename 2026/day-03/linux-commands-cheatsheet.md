## process management commands

To display every active process :
    `ps -ef` and `ps aux` 
    `top` provides real time  interactive view of all processes update every 3 secondsby default (press k within interface to kill the proces)  -d flag to customize the refresh rate .


To kill the process :
    `kill -9 pid`

